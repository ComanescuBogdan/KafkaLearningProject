
server:
  port: 8082

spring:
  application:
    name: payment-service

  config:
    import: "env.properties"

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update